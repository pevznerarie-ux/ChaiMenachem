# ChaiMenachem

Nouveau projet — départ à zéro.

## Structure

- `index.html` — page d'accueil
- `styles.css` — styles
- `app.js` — scripts

## Lancer en local

```bash
npx serve .
```

Puis ouvrir http://localhost:3000
